## SQL Basics – SELECT Statements

### What this section covers
This section introduces the most fundamental part of SQL: **retrieving data from a table** using the `SELECT` statement.

These queries focus on:
- Viewing all data
- Selecting specific columns
- Understanding table structure
- Writing clean and readable queries

This is the foundation for all future SQL topics.

---

### Files in this folder
- `select_queries.sql` – basic SELECT queries using the `customers` table

---

### Key Concepts
- `SELECT`
- Selecting specific columns
- Using `*` to select all columns
- Basic query formatting

